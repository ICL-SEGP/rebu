name: Frontend Tests

on:
  push:
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: "Run tests"
        env: 
          CI: true
        run: |
          ls -A
          npm ci 
          npm test