version: 1
frontend:
    phases:
        prebuild:
            commands:
                - 'npm --prefix app ci'
        build:
            commands:
                - 'npm run --prefix app build'
    artifacts:
        baseDirectory: app/.next
        files:
            - '**/*'
    cache:
        paths: 
           - 'app/node_modules/**/*'
