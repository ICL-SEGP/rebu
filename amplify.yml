version: 1
frontend:
    phases:
        build:
            commands:
                - 'npm --prefix app ci'
                - 'npm run --prefix app build'
    artifacts:
        baseDirectory: app/.next
        files:
            - '**/*'
    cache:
        paths: 
           - 'app/node_modules/**/*'
           - 'app/.next/**/*'
           - 'app/.cache/**/*'
