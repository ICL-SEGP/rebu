defmodule RebuWebApiWeb.SaleJSON do

  @doc """
  Renders a list of sales.
  """
  # def index(%{sales: sales}) do
  #   %{data: for(sale <- sales, do: data(sale))}
  # end

  @doc """
  Renders a single sale.
  """
  # def show(%{sale: sale}) do
  #   %{data: data(sale)}
  # end

  # defp data(%Sale{} = sale) do
  #   %{
  #     id: sale.id,
  #     name: sale.name,
  #     age: sale.age
  #   }
  # end
end
